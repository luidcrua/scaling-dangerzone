// Generated by CoffeeScript 1.6.3
(function() {
  var _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  __Model.Promocion = (function(_super) {
    __extends(Promocion, _super);

    function Promocion() {
      _ref = Promocion.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    Promocion.fields("id", "idComercio", "idCenComercial", "titulo", "imagen", "descripcion", "fechaInicioPublicacion", "fechaFinPublicacion", "gustaUsuario", "meGusta", "superPromo", "cantidad", "promoDestacada", "visible", "fecha", "nombreComercio");

    Promocion.pending = function() {
      return this.select(function(promocion) {
        return !promocion.visible;
      });
    };

    Promocion.finished = function() {
      return this.select(function(promocion) {
        return !!promocion.visible;
      });
    };

    Promocion.byComercio = function(comercioId) {
      return this.select(function(promocion) {
        return promocion.idComercio === comercioId;
      });
    };

    Promocion.byComercioCenCom = function(comercioId, cenComercialId) {
      return this.select(function(promocion) {
        return promocion.idComercio === comercioId && promocion.idCenComercial === cenComercialId;
      });
    };

    Promocion.addPromocion = function(objeto) {
      var buscaPromocion, cantidadProm, descripcionProm, fechaFinPublicacionProm, fechaInicioPublicacionProm, gustaUsuarioProm, idCenComercialProm, idComercioProm, idProm, imagenProm, meGustaProm, promoDestacadaProm, superPromoProm, tituloProm, visibleProm;
      buscaPromocion = __Model.Promocion.findBy("id", parseInt(objeto.id));
      console.log(objeto);
      if (buscaPromocion !== void 0) {
        return console.log("Ya existía");
      } else {
        console.log("NO existía");
        idProm = parseInt(objeto.id);
        idComercioProm = parseInt(objeto.idComercio);
        idCenComercialProm = parseInt(objeto.idCentroComercial);
        tituloProm = objeto.titulo;
        descripcionProm = objeto.descripcion;
        imagenProm = "app/resources/images/promos/" + objeto.imagen;
        gustaUsuarioProm = "heart-empty";
        meGustaProm = "3";
        fechaInicioPublicacionProm = objeto.fechaInicioPublicacionProm;
        fechaFinPublicacionProm = objeto.fechaFinPublicacionProm;
        superPromoProm = objeto.superPromo === "S" ? true : false;
        cantidadProm = parseInt(objeto.cantidad);
        promoDestacadaProm = objeto.promoDestacada === "S" ? true : false;
        visibleProm = objeto.visible === "S" ? true : false;
        return __Model.Promocion.create({
          id: idProm,
          idComercio: idComercioProm,
          idCenComercial: idCenComercialProm,
          titulo: tituloProm,
          descripcion: descripcionProm,
          imagen: imagenProm,
          meGusta: meGustaProm,
          fechaInicioPublicacion: fechaInicioPublicacionProm,
          fechaFinPublicacion: fechaFinPublicacionProm,
          gustaUsuario: gustaUsuarioProm,
          superPromo: superPromoProm,
          cantidad: cantidadProm,
          promoDestacada: promoDestacadaProm,
          visible: visibleProm
        });
      }
    };

    Promocion.prototype.validate = function() {
      if (!this.titulo) {
        "titulo es obligatorio";
      }
      if (!this.imagen) {
        "imagen es obligatorio";
      }
      if (!this.id) {
        return "id es obligatorio";
      }
    };

    return Promocion;

  })(Monocle.Model);

}).call(this);
